services:
  postgres:
    image: postgres:15
    container_name: esat_postgres
    environment:
      POSTGRES_USER: esat
      POSTGRES_PASSWORD: esat
      POSTGRES_DB: esat_db
    ports:
      - "5435:5432"
    volumes:
      - esat_pgdata:/var/lib/postgresql/data

volumes:
  esat_pgdata:
